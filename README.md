# Source collectors

Each source gets its own collector module.

Current:
- `fireroad.py`

Planned:
- `mit_ocw.py`
- `openlearn.py`
- `saylor.py`

All collectors should return the same standardized schema so the datasets can later be merged safely.
